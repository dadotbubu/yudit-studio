# yudit-studio 버그 수정 계획

## 수정 항목 (유디트 요청사항)

### 1. 성과 입력 알림 표기 부분
- **현상**: 성과 입력 알림 표기 이상
- **조사 필요**: 알림 로직, 성과 입력 감지 로직

### 2. 성과 입력해도 요약에 반영 안됨
- **현상**: 성과 데이터 입력 후 요약 섹션에 반영 안됨
- **조사 필요**: 성과 저장 로직, 요약 계산 로직

### 3. 월간비교 - 월간 콘텐츠 성과, 인사이트 업데이트 안됨
- **현상**: 월간 비교 데이터가 업데이트 되지 않음
- **조사 필요**: 월간 집계 로직, 데이터 갱신 로직

### 4. 저장하면 쉼표 찍히는 것
- **현상**: 데이터 저장 시 쉼표가 잘못 들어감
- **조사 필요**: 저장 로직, 입력 필드 처리

### 5. 팔로워 추이 주간 안되는 것
- **현상**: 주간 팔로워 추이가 작동하지 않음
- **조사 필요**: 팔로워 데이터 수집 및 표시 로직

### 6. 대시보드 업로드 된 것 없는데 표기되는 것
- **현상**: 업로드 안된 콘텐츠가 대시보드에 표시됨
- **조사 필요**: 대시보드 필터 로직, 업로드 완료 판단 조건

### 7. 캘린더 작성된 내용으로 연동 안되고 현상태로만 다 연동되는 것
- **현상**: 캘린더에 작성된 콘텐츠가 현재 상태로만 연동됨
- **조사 필요**: 캘린더-콘텐츠 동기화 로직

## ✅ 수정 완료 항목

### 1. 문제 7 - 캘린더 마일스톤 상태 덮어씌워짐
- 수정: `!item.isMilestone` 조건 추가 (3511줄)

### 2. 문제 1 - 성과 입력 알림 전체 콘텐츠 확인
- 수정: `monthContents` → `contentsData.contents` (4033줄)

### 3. 문제 4 - 숫자 쉼표 추가
- 수정: toLocaleString() 추가 (4086, 4094, 4098, 4389, 4392줄)

### 4. 문제 8 - 애플펜슬 글쓰기 방지
- 수정: `touch-action: manipulation` CSS 추가 (index.html 78줄)

### 5. 문제 2, 3 - 성과 요약 재계산
- 추가: recalcMonthPerf() 함수 (3482줄)
- 수정: savePerfCell에서 재계산 호출 (3521줄)

### 6. 문제 5 - 팔로워 주간 추이
- 수정: 실제 주차별 데이터 계산 로직 구현 (4347-4377줄)

## 다음 단계
1. 브라우저에서 테스트
2. 문제 확인
3. 커밋 & 푸시

## 발견된 문제들

### 문제 2, 3 - 성과 입력 후 요약/월간비교 미반영 ⚠️ 심각
**원인**: savePerfCell(3482줄)에서 개별 콘텐츠 performance만 업데이트하고,
performanceData.monthly[month] 요약 데이터를 재계산하지 않음

**해결**: 성과 저장 후 해당 월 요약 재계산 로직 추가 필요

### 문제 5 - 팔로워 추이 주간 안됨 ⚠️ 심각
**원인**: 4296-4300줄, 주간 그래프가 하드코딩 (모두 0)
```javascript
${[0, 0, 0, 0].map((change, idx) => `
  <div class="flex-1 flex flex-col items-center">
    <div class="w-full rounded-t" style="height: 0px; background-color: #8C9A84;"></div>
  </div>
`).join('')}
```

**해결**: 실제 주차별 데이터 계산 및 표시 로직 구현 필요

### 문제 8 (NEW) - 애플펜슬로 표 글쓰기 방지
**요구사항**: "표 글쓰는곳 애플펜슬로 하면 글쓰기가 안되면 좋겠는데 글쓰기가 되더라고 그냥 터치만 되면 되는데"

**해결**: CSS touch-action: manipulation 추가

### 문제 1 - 성과 입력 알림 과거 월에만 표시
**현상**: 현재 5월인데 4월에만 알림 보임

**원인**: renderPerformance(4033줄)에서 needsPerfList를 계산할 때
`monthContents`(선택된 월의 콘텐츠만)에서 필터링
→ 4월 업로드 콘텐츠는 4월 탭에서만 알림 표시됨

**해결**: needsPerfList는 전체 콘텐츠에서 필터링
(업로드 후 2주 지남 + 성과 미입력) → 어느 월이든 항상 표시

### 문제 4 - 성과분석 숫자 쉼표 안 찍힘
**현상**: 45000 → 45,000으로 표시 안됨

**원인**: 4086, 4094줄 등에서 toLocaleString() 누락
- 4094줄: `${monthPerf.totalSaves || 0}` → 쉼표 없음
- 4390줄: `${data.totalSaves?.toLocaleString() || '-'}` → 쉼표 있음 (일부만 적용)

**해결**: 모든 숫자 표시에 toLocaleString() 일관되게 적용

### 문제 6 - 패스 (유디트가 잘못 본 것)

### 문제 7 - 캘린더 마일스톤 상태 연동 오류 ⚠️ 심각
**현상**: 콘텐츠에서 아이디어/기획중/제작중 각각 다른 날짜 입력해도
캘린더에는 3개 날짜 모두 "제작중"으로만 표시됨

**원인**: saveTopInfo(3508-3512줄)에서 content.status 변경 시
모든 연동된 캘린더 항목의 status를 덮어씀
```javascript
calendarData.items.forEach(item => {
  if (item.contentId === contentId) item.status = val; // 마일스톤까지 덮어씀!
});
```

**해결**: 마일스톤 항목은 제외
```javascript
if (item.contentId === contentId && !item.isMilestone) item.status = val;
```

## 현재 상태
- app.js 5313줄 (매우 큼)
- 코드 조사 완료
- 일부 문제 원인 파악 (2, 3, 5, 8)
- 나머지 문제는 유디트 확인 필요
