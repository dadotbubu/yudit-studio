# yudit-studio 기획 탭(프롬프트 생성기) 계획

> ✅ 2026-06-13 구축 완료·배포됨 (모바일 정상, 매우 빠름 확인)
> 이 문서는 확정 스펙 기록용으로 유지 — 변경 시 여기 업데이트

## 구축 완료 내역 (2026-06-13)
- 탭 재구성 + 기획 탭(tab-planning): 생성기 / 레퍼 보관함 / 검색어 3섹션
- 생성기: 카테고리 연동, 포맷 8종, 훅 패밀리 14종 다양성 추첨, 목표별 CTA,
  다른 앵글로·복사·Claude/ChatGPT/제미나이 외부 열기(_system)
- 보관함: 전체/훅/레퍼 토글, 검색+필터+초기화, 훅 H번호, [+훅 추가](Supabase planning_hooks),
  상세 = 훅 응용 템플릿 → 훅 패턴 → 터진 이유 → 원본 대본 + 원본 릴스 링크
- 검색어: 카테고리별 15개, 탭하면 복사
- 데이터: data/planning_data.js (분석본 50개에서 자동 생성)
- 팔로워 일간 그래프 버그 수정 (그리드 통합 + 로컬 시간)

## ⚠️ 운영 교훈 (사고 기록)
- **studio의 data/ 폴더는 .gitignore에 있음** — 새 데이터 파일은 `git add -f` 필수!
  (planning_data.js 배포 누락으로 전체 먹통 났었음, 2026-06-13)
- 스크립트 로딩 절대 체이닝 금지 — 한 파일 실패가 앱 전체를 막으면 안 됨

## 운영 워크플로우
- 유디트가 훅 직접 추가(폰 OK) → 가끔 "훅 태깅해줘" → 앤이 Supabase에서 패턴·템플릿 보강
- 레퍼 추가 분석 → 앤이 planning_data.js 재생성 → git add -f 커밋·푸시
- 레퍼 수집 계속 (50/100, 갭 타겟: 연봉협상·승진 / AI 엑셀·보고서 / 연말정산 / 부부 루틴)

---
(이하 확정 스펙 기록)

## 목표
mignon-web(https://mignon-web.vercel.app/) 같은 릴스 기획 프롬프트 생성기를
스튜디오에 **"기획" 탭**으로 추가. 유디트 세계관 + 레퍼 100개 패턴 내장.

## 1단계: 탭 추가 + 탭 순서/이름 변경

**확정된 탭 구성 (모바일에서 한 줄에 다 보이게):**

```
캘린더 / 플래너 / 콘텐츠 / 기획 / 성과 / 수익 / 노트
```

- 기획 탭 신규 추가 (콘텐츠 뒤, id: tab-planning 등 — 내부 id는 작업 시 확정)
- 성과분석 → **성과** 로 표기 축약
- 수익리포트 → **수익** 로 표기 축약 (현재 모바일만 "수익", 데스크탑도 통일)
- index.html 367-372줄 탭 버튼 영역 수정

## 2단계: 기획 탭 UI (카테고리 드롭다운 하나로 통합 — 유디트 확정)

**구조**: 카테고리 1개 선택 → 포지셔닝·타겟·핵심메시지 자동 연동 (각 필드 수정 가능 + 직접 입력 옵션)

```
[카테고리] ▼  Career Guide / AI Work / Money Log / Life Style / 직접 입력
      ↓ 자동 채움 (수정 가능)
  포지셔닝 / 타겟 독자 / 핵심 메시지
```

### 카테고리 연동표 (확정, 2026-06-12)

| 카테고리 | 포지셔닝 | 타겟 독자 | 핵심 메시지 |
|---|---|---|---|
| Career Guide | 억대연봉 받는 커리어 전문가, 대기업 면접관 | 취준생·사회초년생·이직 희망하는 2~30대 | 면접관 시선에서 알려주는 진짜 합격·커리어 전략 |
| AI Work | 자기계발하는 일잘러, 14년차 대기업 직장인 | AI로 일 잘하고 싶은 직장인 (1~15년차) | 14년차 일잘러의 실전 AI 업무 활용법 |
| Money Log | 월급 관리 잘하고 재테크 잘하는 대기업 직장인 부부 | 자산관리에 관심 있는 2~40대 맞벌이 부부 | 맞벌이 부부의 현실적인 월급 관리·재테크 전략 |
| Life Style | 비싼 거 좋아하지만 전략적으로 소비하는 대기업 직장인 부부 | 대기업 부부 라이프를 동경하는 2~40대 | 따라 하고 싶어지는 전략적 소비·여행·라이프 |

### 나머지 입력

### ★말투 고정 원칙 (유디트 확정, 2026-06-12 — 생성 프롬프트에 항상 포함)
- **레퍼런스가 반말이든 뭐든 무관하게, 대본 전체는 항상 존댓말로 작성**
- 단, ★후킹(첫 3초)은 반말도 OK (유디트 확정, 2026-06-12 — 훅 임팩트 우선)
- **기본 말투 = ★01번 유디트 본인 대본 말투**
  (~예요 / ~거든요 / ~더라고요 + 생동감 있는 감탄 추임새, 1인칭 경험 공유)
- 레퍼의 훅·구조는 가져오되 말투는 유디트 말투로 변환하는 것이 생성기의 고정 규칙

### ★카피 금지 원칙 (유디트 확정, 2026-06-12 — 생성 프롬프트에 항상 포함)
- 같은 주제의 레퍼 **형식(포맷·구조)을 가져오는 것은 OK**
- 단, **처음부터 끝까지 같은 형태 + 같은 훅 = 카피 → 절대 금지**
- 적용 방법:
  · 훅은 패턴(구조)만 가져오고 문장은 유디트 소재·단어로 새로 조합
  · 5파트 중 최소 1~2개 파트는 레퍼와 다른 패턴으로 교차 (전역 훅 풀 활용)
  · 유디트 실제 경험·에피소드(서사라이브러리)를 반드시 1개 이상 주입
    → 경험이 들어가는 순간 구조가 같아도 카피가 아니게 됨
- 세계관 원칙과 연결: 나만의 색 — 레퍼런스는 참고만, 유디트만의 목소리

| 입력 | 방식 | 비고 |
|---|---|---|
| 톤·보이스 | 드롭다운 (확정) | 내 경험 공유 / 정보 전달 / 친근한 수다 / 단호한 조언 + 직접 입력. 레퍼 100개 말투 분석 후 옵션 추가 예정. ※어떤 선택이든 존댓말+유디트 말투 원칙 위에서 변주 |
| 레퍼런스 패턴 | 드롭다운 | 4단계 patterns.json 연동 |
| 길이 | 알약 버튼 | 15초 이내 / 30초 이내 / 60초 이내 / 1분 이상 |
| 목표 | 알약 버튼 | 저장 / 댓글 / 판매·전환 |
| 주제 | 텍스트 입력 | 유일한 자유 입력 (핵심메시지와 별개 — 콘텐츠 소재) |

### 직접 입력 동작 (유디트 확정)
- 카테고리에 "직접 입력" 선택 → 포지셔닝·타겟·핵심메시지 빈칸으로 자유 작성
- 카테고리 선택 시에도 자동 채움된 값 그 자리에서 수정 가능

## 2.5단계: mignon 프롬프트 템플릿 역설계 (완료, 2026-06-12)

유디트가 뽑아준 실제 출력 프롬프트 기준 구조:

```
① 역할 선언      너는 인스타그램 콘텐츠 기획 에이전트다 (고정)
② 계정 컨텍스트   포지셔닝/핵심메시지/타겟/톤보이스/성과패턴 ← 입력값 치환
③ 기획 방법론     (고정 텍스트)
   - 카피 원칙 5개 + 금지 목록
   - 성과 게이트: 3변수 0/1/2 채점 (내 도구·경험/페인포인트/즉시 쓰는 실물)
   - 후킹 뱅크 8패턴: 숫자대비/공감질문/반복나열/진입장벽제거/
     역설반전/손해격차/자가고백/빈칸 (썸네일은 1·6·8 우선)
   - CTA 원칙: 저장→댓글 우선, 1차 행동 단 하나, 댓글 허들 1초 컷,
     경로 분리(1차 공개 댓글 / 2차 뉴스레터·DM)
   - 수치 규칙: 썸네일 카피 ≤15자, 릴스 6파트(썸네일·훅·문제제기·전환·증명/시연·CTA)
④ 이번 기획 조건   길이 + 목표 + 후킹 앵글 3개 랜덤 추첨 ← 다양성의 핵심
⑤ 주제           ← 입력값
⑥ 출력 형식       성과 게이트 채점 → 기획 코어(메시지/훅3안/썸네일/증명/CTA)
                  → 릴스 스크립트(6파트+대사·자막·B-roll 메모) → HUMAN CHECK 2~3개
```

**재생성 다양성의 정체**: ④에서 8패턴 중 3개 랜덤 추첨이 전부. 고정 템플릿 + 치환 + 랜덤이라 클라이언트 JS로 구현 간단.

**UI 디테일**: 길이/목표 알약 버튼, (다른 앵글로) 재추첨 버튼, 복사 버튼, Claude/ChatGPT 바로 열기 링크, 프로필 localStorage 저장.

**유디트 버전 차별점**:
- 계정 컨텍스트 → 카테고리 연동표로 자동 삽입
- 후킹 뱅크 8패턴 + 레퍼 100개 추출 검증 패턴 추가
- 성과 패턴 = 레퍼 풀 자체 (유디트가 수집한 레퍼 전부 시장 압도 콘텐츠 — 별도 본인 패턴 슬롯 불필요, ★01은 말투 기준으로만 사용. 2026-06-13 확정)
- 후킹_마스터_가이드 원칙(단어 충돌, 콘텐츠 필터: 대기업 부부 필수) 방법론에 반영
- ★스크립트 구조: mignon 6파트 대신 유디트 5파트 사용
  → 후킹(앞 3초) / (인트로) / 메인 / (아웃트로) / CTA
  → 생성 프롬프트의 출력 형식도 이 5파트 기준
  → 파트별 패턴 라이브러리에서 조합 주입 (훅 X 메인 구조 X CTA)

- 출력: 기획 프롬프트 생성 → 복사 버튼 (ChatGPT/Claude에 붙여넣기용)
- 재생성 시 훅 앵글 랜덤 변형 (mignon의 다양성 기능 재현)
- 전부 클라이언트 사이드 (서버 불필요, GitHub Pages 그대로)
- 드롭다운 옵션은 `data/prompt_presets.json` 분리 (추후 JSON만 갱신)

## 2.7단계: 기획 탭 내 "레퍼 보관함" (유디트 확정)

기획 탭을 두 영역으로 구성:

```
기획 탭
├── 프롬프트 생성기 (메인)
├── 레퍼 보관함
│   ├── 포맷별 필터 (비포vs애프터 / 꿀팁N개 / ...)
│   ├── 카테고리별 필터 (아래 보관함 전용 분류)
│   ├── 검색 (주제 키워드 매칭)
│   └── 레퍼 카드: 원본 대본 + 5파트 분해 + 훅 분석
│       + ★원본 릴스 링크 버튼 (영상·화면 연출 다시 보기, 2026-06-12 추가)
│         — 분석본마다 링크 필드, SRT 수동 추출분(1~11)은 유디트가 링크 제공 시 백필
└── ★검색어 추천 (유디트 확정 추가, 2026-06-12)
    ├── 용도: 인스타에서 베스트 레퍼 영상 찾을 때 검색어 고민 해결
    ├── 카테고리(4개)별 추천 검색어 목록 표시
    ├── ★형태: 단어 형태 위주 (유디트 확정 — 문장형 X)
    │   예: 면접 / 자소서 / 통장쪼개기 / 챗GPT / 신혼집
    ├── 데이터 소스 2종:
    │   ① 앤 큐레이션 검색어 (시작 세트 — 채팅으로 이미 제공)
    │   ② 레퍼 분석 때 태깅한 주제 키워드 재활용
    │   (잘 된 레퍼의 키워드 = 검증된 검색어 → 레퍼 쌓일수록 자동 강화)
    └── 사용한 검색어 체크 표시 (뭘로 찾아봤는지 기록)
```

**보관함 카테고리 (유디트 확정) — 계정 카테고리보다 넓게:**

| 보관함 카테고리 | 범위 | 계정 카테고리 대응 |
|---|---|---|
| 커리어&자기계발 | 취업·이직·승진·자기계발 | Career Guide |
| 재테크&부동산 | 월급관리·투자·부동산·집 | Money Log |
| AI | AI 활용·업무 자동화 | AI Work |
| 라이프 | 쇼핑·여행·집·일상 | Life Style |
| ★기타 (2026-06-12 추가) | 채널과 무관한 주제 (독서·요리·반려동물 등) | 대응 없음 — 훅·대본 구조만 참고 |

※ 기타 레퍼 처리 규칙: 훅 정밀 분석·5파트·포맷 분류는 패턴 라이브러리에 포함,
  주제 키워드는 검색어 추천 강화에서 제외
※ 자막만 있는 릴스(대사 없음)는 추출 불가 → 유디트가 따로 줌 (스크린샷 등)
  → 분석본·보관함에 "자막만 있음" 표시 태그 (분석은 동일하게)
  → ★자막형은 별도 생성 트랙 없음 (2026-06-13 확정) — 대본/자막 선택은 유디트가 제작 시 결정.
    생성기는 아주 짧은 형태(초단축 5~15초) 제안만 가능

**훅 리스트 뷰 (기본 화면) — 유디트 확정 목업:**

```
┌─────────────────────────────────────┐
│ 검색: [          ]  [포맷▼] [카테고리▼]  │
├─────────────────────────────────────┤
│ 이런 분들 절대 컴퓨터방 따로 두지 마세요    │
│   금지형 · 라이프 · 39초                │
├─────────────────────────────────────┤
│ (훅 첫 줄만 쫙 — 100개 스크롤 훑어보기)   │
└─────────────────────────────────────┘
        ↓ 훅 탭하면
│ [상세] 원본 대본 + 5파트 분해 + 분석      │
```

**검색 = 주제 키워드 매칭:**
- 분석 시 레퍼마다 주제 키워드 태깅 (예: 이사, 집구조, 에어컨, 신혼집)
- 훅 문장에 없는 단어도 키워드로 검색 가능 + 훅 텍스트 검색도 지원

**데이터 설계 (가볍게):**
- 생성기는 patterns.json만 사용 (~20-50KB) → 탭 열 때 즉시 로딩
- 레퍼 원본+분석(100개 약 400-500KB)은 보관함 열 때만 lazy load
- 앱 시작 로딩에 영향 없게 분리 (Supabase 별도 key)
- 폰(PWA)에서도 언제든 열람 가능

## 3단계: 레퍼런스 100개 → 프롬프트화 파이프라인

```
① 수집     유디트가 대본 공유 (10개씩, 전부 검증된 잘 된 레퍼만 줌
            → 성과 수치 불필요, 카테고리 느낌만 있으면 좋음)
            → yu_reels/references/에 저장
② 포맷 분류  ★100개를 먼저 콘텐츠 형태로 분류 (유디트 확정 방식)
            예: 비포vs애프터 / 꿀팁 N개 공유 / 금지형 / 경험담 스토리...
            (실제 분류 체계는 대본 보고 도출, 몇 개로 나뉘는지 보고)
③ 파트 분해  포맷별로 대본을 5파트로 분해 후 태깅:
            후킹(앞 3초) / (인트로) / 메인 / (아웃트로) / CTA
            (괄호 파트는 없을 수 있음 — 유무 자체도 태깅)
            + 훅 유형 / 길이 / 감정톤 / 말투(어미·호칭·문장 리듬)
            + 보관함 카테고리 + 주제 키워드 (보관함 검색용)

   ★훅 정밀 분석 (유디트 강조 — 모든 레퍼 필수 섹션):
   - 훅 문장 전문 + 패턴 분류
   - 단어 충돌 요소 (어떤 단어끼리 부딪히나)
   - 첫 1.5초에 들리는/보이는 단어 (스크롤 멈추는 지점)
   - 왜 멈추게 하나 (심리 장치: 손해회피/호기심/내얘기인가)
   - 훅 응용 템플릿 (이 훅을 유디트 소재로 변형)
   → 100개 완료 시 즉시 사용 가능한 훅 템플릿 100개 확보
④ 패턴 추출  ★같은 포맷 안에서 파트별 클러스터링
            → 포맷별 훅 패턴 / 인트로 / 메인 구조 / 아웃트로 / CTA
            + 말투 프로파일 (포맷 공통)
⑤ 프롬프트화 patterns.json 생성 (포맷 → 파트 계층 구조)
            → 생성기 UI의 레퍼런스 패턴 드롭다운 = ★포맷 선택
            → 조합 규칙 (2026-06-12 유디트 수정):
              · 훅 = ★전역 풀 — 카테고리·기타 무관, 전체 훅 템플릿에서 추첨
                (훅은 심리 장치라 주제 경계 없음. 기타 레퍼 훅도 응용 템플릿으로 활용)
              · 메인 구조 = 포맷 단위 (구조는 포맷과 한 몸)
              · CTA = 목표 기준 (저장/댓글/판매 선택에 따라)
            말투 프로파일 → 톤보이스 드롭다운 옵션으로 추가

   ★(다른 앵글로) 재추첨 로직 (유디트 확정):
   - 사용한 조합은 세션 내 이력으로 기억 (중복 방지)
   - 1순위: 같은 포맷 안에서 아직 안 쓴 조합 (훅은 전역에서)
   - 2순위: 포맷 내 조합 소진 시 다른 포맷의 조합 가져오기
⑥ 통합     유디트_훅_라이브러리·후킹_마스터_가이드에 새 패턴 반영
```

## 레퍼 수집 워크플로우 (확정·가동 중)

- 전달 방식 2가지:
  ① SRT 파일 → /Users/youjeongkim/Desktop/래퍼런스/에 번호로 추가
  ② 릴스 링크 → 앤이 로컬 추출 (yt-dlp + mlx_whisper, 설치 완료)
- 링크 범위 규칙: 링크만 오면 "추출만? 분석까지?" 확인
  / `추출만 [링크]` → SRT만 / `분석 [링크]` → 풀코스
- 코덱스용 스크립트: yu_reels/릴스추출.sh (추출만 담당)
- ※ 추출 기능은 기획 탭에 포함 불가 (정적 사이트 — 서버 필요 작업)
  모바일에서는 외부 사이트 사용 or 링크 모아뒀다 맥에서 일괄 처리
- 분석 산출물 위치:
  - 분석본: yu_reels/references/20YYMMDD_NN_제목.md (훅 정밀 분석 포함)
  - 진행 현황: yu_reels/references/_분석현황.md
  - 검색어 100 시작 세트: yu_reels/references/_검색어추천.md

## 대기 중 콘텐츠 아이디어 (레퍼 100개 완료 후 진행)

**"릴스 대본 무료 추출기" 릴스 (AI Work 카테고리)**
- 타겟: 릴스 부업 시작했거나 하려는 직장인
- 훅 후보: 금지형(돈 내지 마세요) / 숫자대비(월 3만원→0원) /
  권위(레퍼 100개 분석하는 제가 쓰는 방법) / 운명형(이 영상이 떴다면)
- 포맷: 리드수집형 — 시연 → 댓글 키워드 [대본] → 노션 가이드 DM
- 자료 완성됨: 노션 페이지 (맥 가이드 + 윈도우 AI 프롬프트 통합)
  https://app.notion.com/p/37d066f5322281c4b6dae8a08b0e1487
- 유디트 컨펌: 레퍼런스 분석 다 끝낸 뒤 앤이랑 대본 작업 (2026-06-12)

## 미확정 사항 (유디트 확인 필요)
1. 기획 탭 내부 id 영문명 (tab-planning 등) — 작업 시작 시 확정

## 작업 순서 (시작 승인 후)
1. 탭 순서/이름 변경 + 기획 탭 골격
2. 드롭다운 프리셋 초안 → 유디트 컨펌
3. 프롬프트 템플릿 엔진 구현 → 목업 먼저 보여주기
4. 레퍼 수집 시작되면 파이프라인 병행
5. 완료 후 커밋·푸시 (자동)
